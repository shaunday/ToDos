<UserControl x:Class="Todos.Ui.Sections.Tasks.TaskFilterControl"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="72" d:DesignWidth="900">
    <Border Padding="16" Background="{DynamicResource MaterialDesignPaper}" CornerRadius="12" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,12">
        <Grid VerticalAlignment="Center" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Priority Filter -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,0">
                <materialDesign:PackIcon Kind="Flag" Width="20" Height="20" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,6,0"/>
                <TextBlock Text="Priority" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <ComboBox Width="120"
                          Style="{DynamicResource MaterialDesignOutlinedComboBox}"
                          SelectedValue="{Binding Priority, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          SelectedValuePath="Content"
                          materialDesign:HintAssist.Hint="Priority"
                          Margin="0,0,0,0">
                    <ComboBoxItem Content="All" />
                    <ComboBoxItem Content="High" />
                    <ComboBoxItem Content="Medium" />
                    <ComboBoxItem Content="Low" />
                </ComboBox>
            </StackPanel>

            <!-- Divider -->
            <Rectangle Grid.Column="1" Width="1" Height="36" Fill="#E0E0E0" VerticalAlignment="Center"/>

            <!-- Tag Filter -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,0">
                <materialDesign:PackIcon Kind="Tag" Width="20" Height="20" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,6,0"/>
                <TextBlock Text="Tag" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <TextBox Width="140"
                         Style="{DynamicResource MaterialDesignOutlinedTextBox}"
                         Text="{Binding TagFilter, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Filter by tag"
                         Margin="0,0,0,0"/>
            </StackPanel>

            <!-- Divider -->
            <Rectangle Grid.Column="3" Width="1" Height="36" Fill="#E0E0E0" VerticalAlignment="Center"/>

            <!-- Completed Filter -->
            <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,0">
                <materialDesign:PackIcon Kind="CheckCircleOutline" Width="20" Height="20" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,6,0"/>
                <TextBlock Text="Status" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <ComboBox Width="130"
                          Style="{DynamicResource MaterialDesignOutlinedComboBox}"
                          SelectedValue="{Binding CompletedStatus, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          SelectedValuePath="Content"
                          materialDesign:HintAssist.Hint="Status"
                          Margin="0,0,0,0">
                    <ComboBoxItem Content="All" />
                    <ComboBoxItem Content="Completed" />
                    <ComboBoxItem Content="Not Completed" />
                </ComboBox>
            </StackPanel>

            <!-- Spacer -->
            <StackPanel Grid.Column="5"/>

            <!-- Buttons -->
            <StackPanel Grid.Column="6" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,0,0">
                <Button Style="{DynamicResource MaterialDesignContainedButton}"
                        Content="Clear All Filters"
                        Click="ClearAllFilters_Click"
                        MinWidth="140"
                        Padding="16,6"
                        FontWeight="SemiBold"
                        Margin="0,0,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl> 