<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataTemplate x:Key="StatusDataTemplate">
        <ItemsControl HorizontalAlignment="Center" VerticalAlignment="Center">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.Items>
                <!-- Editing Status -->
                <ContentControl Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ContentControl.ContentTemplate>
                        <DataTemplate>
                            <Border Background="Green" 
                                    CornerRadius="4" 
                                    Padding="3,1" 
                                    Height="24"
                                    VerticalAlignment="Center">
                                <TextBlock Text="EDITING..." 
                                           FontSize="8" 
                                           FontWeight="Normal" 
                                           Foreground="White"
                                           VerticalAlignment="Center" />
                            </Border>
                        </DataTemplate>
                    </ContentControl.ContentTemplate>
                </ContentControl>
                
                <!-- Locked Status -->
                <ContentControl Visibility="{Binding IsLocked, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ContentControl.ContentTemplate>
                        <DataTemplate>
                            <Border Background="Red" 
                                    CornerRadius="4" 
                                    Padding="3,1" 
                                    Height="24"
                                    VerticalAlignment="Center">
                                <TextBlock Text="LOCKED" 
                                           FontSize="8" 
                                           FontWeight="Normal" 
                                           Foreground="White"
                                           VerticalAlignment="Center" />
                            </Border>
                        </DataTemplate>
                    </ContentControl.ContentTemplate>
                </ContentControl>
                
                <!-- Completed Status -->
                <ContentControl Visibility="{Binding IsCompleted, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ContentControl.ContentTemplate>
                        <DataTemplate>
                            <Border Background="Blue" 
                                    CornerRadius="4" 
                                    Padding="3,1" 
                                    Height="24"
                                    VerticalAlignment="Center">
                                <TextBlock Text="DONE" 
                                           FontSize="8" 
                                           FontWeight="Normal" 
                                           Foreground="White"
                                           VerticalAlignment="Center" />
                            </Border>
                        </DataTemplate>
                    </ContentControl.ContentTemplate>
                </ContentControl>
            </ItemsControl.Items>
        </ItemsControl>
    </DataTemplate>
</ResourceDictionary> 