<UserControl x:Class="Todos.Ui.Panels.TopPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:Todos.Ui.Resources.Converters"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="1200">

    <UserControl.Resources>
        <converters:ConnectionStatusToColorConverter x:Key="ConnectionStatusToColorConverter" />
    </UserControl.Resources>

    <Border Background="{DynamicResource MaterialDesignPaper}"
            BorderBrush="{DynamicResource MaterialDesignDivider}"
            BorderThickness="0,0,0,1"
            Padding="16,12"
            Effect="{DynamicResource MaterialDesignElevationShadow2}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Connection Status -->
            <StackPanel Grid.Column="0" 
                        Orientation="Horizontal" 
                        VerticalAlignment="Center">
                <Ellipse Width="8" 
                         Height="8" 
                         Margin="0,0,8,0"
                         Fill="{Binding ApplicationViewModel.ConnectionStatus, Converter={StaticResource ConnectionStatusToColorConverter}}" />
                <TextBlock Text="{Binding ApplicationViewModel.ConnectionStatusText}" 
                           FontWeight="Medium"
                           VerticalAlignment="Center" />
            </StackPanel>

            <!-- User Data -->
            <StackPanel Grid.Column="1" 
                        Orientation="Horizontal" 
                        VerticalAlignment="Center">
                <materialDesign:PackIcon Kind="Account" 
                                         Width="16" 
                                         Height="16" 
                                         Margin="0,0,8,0"
                                         VerticalAlignment="Center" />
                <TextBlock Text="{Binding ApplicationViewModel.CurrentUser.DisplayName}" 
                           FontWeight="Medium"
                           Margin="0,0,16,0"
                           VerticalAlignment="Center" />
                <TextBlock Text="(" 
                           VerticalAlignment="Center" />
                <TextBlock Text="{Binding ApplicationViewModel.CurrentUser.Role}" 
                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                           VerticalAlignment="Center" />
                <TextBlock Text=")" 
                           VerticalAlignment="Center" />
                
                <!-- Logout Button -->
                <Button Style="{DynamicResource MaterialDesignIconButton}"
                        Command="{Binding ApplicationViewModel.LogoutCommand}"
                        ToolTip="Logout"
                        Margin="16,0,0,0">
                    <materialDesign:PackIcon Kind="Logout" 
                                             Width="16" 
                                             Height="16" />
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl> 