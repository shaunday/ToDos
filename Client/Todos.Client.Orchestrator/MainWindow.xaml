<Window x:Class="Todos.Client.Orchestrator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:controls="clr-namespace:Todos.Client.Orchestrator.Controls"
        xmlns:common="clr-namespace:Todos.Client.Common;assembly=Todos.Client.Common"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Padding="5"
        Title="Orchestrator" Height="1200" Width="1600" 
        MinHeight="600" MinWidth="1200"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <ObjectDataProvider x:Key="ClientTypeValues"
                            MethodName="GetValues"
                            ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="common:TypesGlobal+ClientType"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignHeadline5TextBlock}">
            <Setter Property="Margin" Value="0,0,0,24" />
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
        </Style>
        <Style TargetType="GroupBox" BasedOn="{StaticResource MaterialDesignGroupBox}">
            <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignDivider}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="Margin" Value="0,0,0,12" />
            <Setter Property="MinHeight" Value="80" />
        </Style>
        <!-- Centered TextBlock Style -->
        <Style x:Key="CenteredTextBlockStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>
        <!-- Centered ComboBox Style -->
        <Style x:Key="CenteredComboBoxStyle" TargetType="ComboBox">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>
        <!-- Centered ContentControl Style -->
        <Style x:Key="CenteredContentControlStyle" TargetType="ContentControl">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>
    </Window.Resources>
    <Grid Background="{DynamicResource MaterialDesignPaper}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/> <!-- Sidebar for controls -->
            <ColumnDefinition Width="700"/> <!-- Client list, constant width -->
            <ColumnDefinition Width="*"/>   <!-- Log viewer, takes remaining space -->
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Sidebar Controls -->
        <StackPanel Grid.Column="0" Margin="4,8,0,8" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
            <materialDesign:Card Padding="8" Margin="0,0,0,8" materialDesign:ElevationAssist.Elevation="Dp1">
                <StackPanel>
                    <TextBlock Text="Orchestrator Ui Launcher" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Margin="0,0,0,20"/>
                    <DockPanel Margin="0,0,0,8">
                        <CheckBox Content=""
                                  IsChecked="{Binding LaunchByFileConfig, Mode=TwoWay}"
                                  Style="{StaticResource MaterialDesignCheckBox}"
                                  VerticalAlignment="Center"
                                  Margin="0,0,8,0"/>
                        <TextBlock Text="Launch by file config"
                                   VerticalAlignment="Center"
                                   FontWeight="SemiBold"
                                   FontSize="14"
                                   Foreground="{DynamicResource MaterialDesignBody}"/>
                    </DockPanel>
                    <StackPanel x:Name="FileConfigPanel" Orientation="Horizontal" Margin="0,0,0,8" Visibility="{Binding LaunchByFileConfig, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="File path:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox Text="{Binding FileConfigPath, Mode=TwoWay}" Width="140" Margin="0,0,0,0"/>
                    </StackPanel>
                    <TextBlock x:Name="FileConfigDesc" Text="{Binding FileConfigDescription}" TextWrapping="Wrap" FontSize="11" Foreground="Gray" Margin="0,0,0,12" Visibility="{Binding LaunchByFileConfig, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    <Button x:Name="LaunchByFileConfigButton" Content="Launch UiClients by File Config" Style="{StaticResource MaterialDesignRaisedButton}" Command="{Binding LaunchClientsByFileConfigCommand}" HorizontalAlignment="Stretch" Margin="0,0,0,12" Visibility="{Binding LaunchByFileConfig, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    <!-- Normal controls, hidden when LaunchByFileConfig is true -->
                    <Grid x:Name="ClientTypeGrid" Margin="0,4,0,8" Visibility="{Binding LaunchByFileConfig, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Client Type:" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="Ui Client" FontSize="14" VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </Grid>
                    <Grid x:Name="ClientCountGrid" Margin="0,0,0,8" Visibility="{Binding LaunchByFileConfig, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Count:" VerticalAlignment="Center"/>
                        <TextBox x:Name="ClientCountTextBox" Grid.Column="1" Text="{Binding LaunchClientCount, Mode=TwoWay}" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Margin="8,0,0,0"/>
                    </Grid>
                    <Button x:Name="LaunchClientButton" Content="Launch UI Client(s)" Style="{StaticResource MaterialDesignRaisedButton}" Command="{Binding LaunchClientCommand}" HorizontalAlignment="Stretch" Margin="0,4,0,8" Visibility="{Binding LaunchByFileConfig, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                </StackPanel>
            </materialDesign:Card>
            <materialDesign:Card Padding="8" Margin="0,0,0,8" materialDesign:ElevationAssist.Elevation="Dp1">
                <StackPanel>
                    <TextBlock Text="Client Simulators Launcher" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Margin="0,0,0,12"/>
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,8">
                        <TextBlock Text="Client Type:" VerticalAlignment="Center"/>
                        <TextBlock Text="Client Simulator" FontSize="14" VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </StackPanel>
                    <Grid Margin="0,0,0,8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Count:" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SimLaunchCount, Mode=TwoWay}" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Margin="8,0,0,0"/>
                        <TextBlock Text="User ID:" VerticalAlignment="Center" Grid.Row="1" Grid.Column="0"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SimUserId, Mode=TwoWay}" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Margin="8,0,0,0"/>
                        <TextBlock Text="Folder Path:" VerticalAlignment="Center" Grid.Row="2" Grid.Column="0"/>
                        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                            <TextBox Text="{Binding SimFolderPath, Mode=TwoWay}" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Width="180"/>
                        </StackPanel>
                    </Grid>
                    <Button Content="Open Example File" Style="{StaticResource MaterialDesignOutlinedButton}" Command="{Binding OpenSimExampleFileCommand}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,4" Width="180" Height="28" Grid.ColumnSpan="2"/>
                    <TextBlock Text="{Binding SimLauncherInstructions}" FontSize="11" Foreground="Gray" Margin="0,0,0,8" TextWrapping="Wrap" MaxWidth="210"/>
                    <Button Content="Launch Client Simulation(s)" Style="{StaticResource MaterialDesignRaisedButton}" Command="{Binding LaunchClientSimCommand}" HorizontalAlignment="Stretch" Margin="0,4,0,8" Height="36"/>
                </StackPanel>
            </materialDesign:Card>
            <materialDesign:Card Padding="8" materialDesign:ElevationAssist.Elevation="Dp1">
                <StackPanel>
                    <TextBlock Text="Filtering" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Margin="0,0,0,12"/>
                    <Grid Margin="0,0,0,8" >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <!-- Client Type Filter -->
                        <DockPanel Margin="0,0,0,8" >
                            <CheckBox x:Name="IsAliveCheck"
                                      Content=""
                                      IsChecked="{Binding FilterIsAlive, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      Style="{StaticResource MaterialDesignCheckBox}"
                                      VerticalAlignment="Center"
                                      Margin="0,0,8,0"/>
                            <TextBlock Text="Only Live Instances"
                                       VerticalAlignment="Center"
                                       FontWeight="SemiBold"
                                       FontSize="14"
                                       Foreground="{DynamicResource MaterialDesignBody}"/>
                        </DockPanel>
                        <TextBlock Text="Client Type:" VerticalAlignment="Center" Grid.Row="1" Grid.Column="0" Margin="0,0,0,2"/>
                        <ComboBox x:Name="ClientTypeCombo" Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Source={StaticResource ClientTypeValues}}" SelectedItem="{Binding FilterClientType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsEditable="False" Margin="8,0,0,2"/>

                        <!-- PID Filter -->
                        <TextBlock Text="PID:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="8,0,0,0"/>
                        <TextBox Text="{Binding FilterProcessIdText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" VerticalContentAlignment="Center" Margin="8,0,0,2"/>
                    </Grid>
                    <Button Content="Clear All Filters" Style="{StaticResource MaterialDesignOutlinedButton}" Command="{Binding ClearFiltersCommand}" HorizontalAlignment="Stretch" Margin="0,4,0,8"/>
                    <!-- Kill All Clients button, only visible if there are clients -->
                    <Button x:Name="KillAllClientsButton"
                            Content="Kill All Clients"
                            Command="{Binding KillAllClientsCommand}"
                            HorizontalAlignment="Stretch"
                            Margin="0,8,0,0">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignOutlinedButton}">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding TotalClientCount}" Value="0">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>

        <!-- Client List: remains large and central -->
        <controls:ClientListControl Grid.Column="1" Margin="4,8,4,8" />

        <!-- Log Viewer: now on the far right, takes remaining space, reduced margin -->
        <materialDesign:Card Grid.Column="2" Margin="4,8,4,8" Padding="8" materialDesign:ElevationAssist.Elevation="Dp2" VerticalAlignment="Stretch">
            <controls:LogViewerControl x:Name="LogViewer"/>
        </materialDesign:Card>
    </Grid>
</Window>
