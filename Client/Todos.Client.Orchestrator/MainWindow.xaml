<Window x:Class="Todos.Client.Orchestrator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Orchestrator" Height="350" Width="525">
    <Grid Background="{DynamicResource MaterialDesignPaper}">
        <materialDesign:Card Margin="32" Padding="24">
            <StackPanel>
                <TextBlock Text="Orchestrator Launcher" FontSize="24" Margin="0,0,0,16" />
                <Button Content="Launch Client" Style="{StaticResource MaterialDesignRaisedButton}" Width="150" Height="40" Command="{Binding LaunchClientCommand}" />
                <TextBlock Text="{Binding RunningCount, StringFormat=Running: {0}}" Margin="0,16,0,8" FontWeight="Bold" />
                <ListBox ItemsSource="{Binding RunningClients}" Height="120" Margin="0,0,0,8">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="PID: " />
                                <TextBlock Text="{Binding Id}" FontWeight="Bold" Margin="0,0,8,0" />
                                <TextBlock Text="{Binding MainWindowTitle}" Margin="0,0,8,0" />
                                <Button Content="Kill" Command="{Binding DataContext.KillClientCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Style="{StaticResource MaterialDesignFlatButton}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Window>
